apiVersion: "k8s.cni.cncf.io/v1"
kind: NetworkAttachmentDefinition
metadata:
  name: {{ name }}
  namespace: {{ namespace }}
spec:
  config: '{
      "cniVersion": "0.3.1",
      "type": {{ cni }},
      {{ vlan }}
      "bridge": {{ bridge }}
    }'
