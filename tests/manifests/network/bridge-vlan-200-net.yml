apiVersion: "k8s.cni.cncf.io/v1"
kind: NetworkAttachmentDefinition
metadata:
  name: br1vlan200
  namespace: linux-bridge-connectivity
spec:
  config: '{
      "cniVersion": "0.3.1",
      "type": "{{ bridge_cni_name }}",
      "bridge": "br1test",
      "vlan": 200
    }'
