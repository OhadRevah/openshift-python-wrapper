#cloud-config
password: fedora
chpasswd: { expire: False }
ssh_pwauth: True
bootcmd:
  - dnf install -y tcpdump qemu-guest-agent iperf3 dmidecode nginx lldpad kernel-modules nmap dhcp stress
runcmd:
  - systemctl start qemu-guest-agent
